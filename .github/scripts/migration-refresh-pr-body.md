Opened automatically by `scheduled-migration-refresh.yaml` -- the chart's own
constraints.tpl helpers changed since this data was last generated.

**Before merging:** read the diff, not just the stat below -- a consolidation
(several checks merged into one) looks similar to a real gap (coverage quietly
dropped) in a line-count summary alone. Run the full suite and spot-check
`camunda-helm-toolkit upgrade` against a values file that actually exercises
whatever changed.
